# AGENTS.md

## 目标

当前工作区包含两个核心目录：

- `SlowRenju/`：现有的 C 语言五子棋/连珠引擎，作为参考项目。
- `pyslow/`：准备新建的 Python 五子棋项目目录。

`pyslow` 的目标是：用 Python 实现一个有实际棋力的五子棋引擎，整体架构、模块划分、参数配置可移植自 `SlowRenju`，但实现应以 Python 项目自身的可维护性和可扩展性为主。

目标要求：

- 第一阶段是支持自由五子棋（freestyle Gomoku）。
- 第一阶段只需要支持15*15的标准棋盘
- 目标不是演示程序，而是具备不错实战棋力的引擎。
- 需要添加VCF/VCT等战术模块杀棋搜索，并且战术模块的候选点，与普通搜索分开，杀棋搜索：只生成威胁点 / 防守点，不看普通邻域规则
- 设计时要考虑后续将性能热点迁移到 native 模块。
- docs/目录下是按照参考项目编写的本项目规格书，需要先读取这些文档，并按文档要求实施
- 涉及 reference 对齐时，优先使用最小 reference trace 做分支级对拍，不要只看主流程相似性

## 非目标

- 不做 C 到 Python 机械翻译。
- 主体设计按参考项目来移植，特别是评估和搜索的功能及关键参数需要完整借鉴。
- 不在项目初期做全面过度优化。
- 在引擎核心不可用之前，不优先做 GUI。

## 参考项目中的关键信息

根据 `SlowRenju` 的 README 和头文件，当前已确认的关键点：

- 核心搜索框架是迭代加深 `alpha-beta`。
- 使用置换表（transposition table）。
- 支持 `5` 到 `20` 路棋盘。
- 支持自由五子棋、标准五子棋、连珠。
- 评估与搜索中存在一组集中管理的可调参数。
- 模块上区分了搜索、棋形识别、评估、VCF、协议处理等部分。



## 推荐实现顺序

建议按下面的顺序推进：

1. 棋盘表示、落子/悔棋、终局判断、zobrist 哈希。
2. 基于已有棋子邻域的候选点生成。
3. 棋形提取与可用的静态评估。
4. 带置换表的迭代加深 alpha-beta 搜索。
5. 协议适配，使引擎可以实际对弈。
6. 威胁搜索、VCF 、VCT等战术增强。
7. 在确认瓶颈后，再进行 native 加速。

## 性能策略

默认先做纯 Python，但结构上要为后续native加速预留空间。

推荐流程：

1. 先保证 Python 版本正确、可运行、可测量。
2. 加入 benchmark 和 profiling。
3. 只把真正热点迁移到 native 模块。

高概率热点包括：

- 棋形/模式扫描
- 候选点生成
- 局部评估循环
- 置换表探测与键值处理
- 威胁搜索

优先考虑的 native 方案：

1. `Cython`，适合数组和紧密循环。
2. `PyO3` / Rust，或小型 C/C++ 扩展，适合隔离出的热点模块。
3. `Numba`，仅在数据布局适合且维护成本可接受时考虑。

在 benchmark 没证明瓶颈之前，不要提前引入 native 代码。

## 参数策略

参考 `SlowRenju` 的“参数集中管理”思路，但在 Python 中要表达得更清晰。

要求：

- 所有参数集中在 `config.py`。
- 评估权重与搜索控制参数分开。
- 后续支持从文件或命令行覆盖参数。
- 每个参数要有明确名称和含义。
- 避免把 magic number 散落在搜索和评估逻辑中。

建议参数分组：

- 完全按参考项目的参数分组

## 工程规则

- 优先交付能运行的增量版本。
- 所有搜索特性都必须保证悔棋和哈希状态一致。
- 为棋盘合法性、哈希稳定性、胜负检测、搜索回归添加测试。
- 除非显式需要随机性，否则引擎行为应保持确定性。
- 先 profile，再优化。

## 第一阶段里程碑

`pyslow` 的第一阶段目标应当是：

- 一个可在命令行运行的五子棋引擎
- 支持 `15x15` 自由五子棋
- 具备迭代加深 alpha-beta 搜索
- 具备基础置换表
- 具备局部候选点生成
- 具备可配置评估参数
- 具备VCF/VCT战术增强

## 在本仓库中的工作方式

后续进行实现时，应遵循以下原则：

- 阅读 `SlowRenju` 时，重点关注架构、术语、参数类别和行为设计。
- 新逻辑只在 `pyslow` 中实现。
- 每次修改尽量只覆盖一个子系统，避免一次性铺太大。
- 每实现一个功能，都需要补充对应的测试项目，测试项目要保证覆盖性
- 如果性能成为问题，先补 benchmark，再决定是否引入 native 加速。
